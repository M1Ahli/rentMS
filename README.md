# Property Management System (Offline) — نظام إدارة العقارات (محلي)

**الإصدار النهائي:** UIV32.22 (Hotfix HTTP/NAS)
**تاريخ الحزمة:** 2026-02-04
**التشغيل:** Offline داخل المتصفح عبر Local Web Server (مثل Live Server أو خادم الـ NAS)  
**التقنيات:** HTML + CSS + Vanilla JavaScript + Tailwind (CDN)  
**اللغة:** واجهة عربية RTL + دعم الوضع الليلي/النهاري + تصميم زجاجي موحّد

---

## 1) فكرة النظام باختصار
هذا النظام مصمم لإدارة عقاراتك محليًا بدون أي سيرفر خلفي. يتم حفظ البيانات في المتصفح (localStorage) مع إمكانيات **تصدير/استيراد نسخة احتياطية** بصيغة JSON.  
تم تصميم الواجهات بشكل موحّد (Glassmorphism) مع **بحث/فرز** وفلاتر متقدمة في الصفحات الأساسية.

---

## 2) التشغيل (مهم جدًا)
> ⚠️ لا تشغّل النظام عن طريق فتح الملف مباشرة (file://) لأن المشروع يعتمد على `fetch()` لتحميل الصفحات والمكونات.

### خيار A: Live Server (Visual Studio Code)
1. افتح مجلد المشروع في VS Code  
2. افتح `index.html`  
3. شغّل **Live Server**  
4. افتح الرابط مثل: `http://127.0.0.1:5500/restructured_step184/index.html`

### خيار B: على الـ NAS
ارفع مجلد `restructured_step184` إلى مسار يستضيفه خادم الويب في جهاز الـ NAS ثم افتح `index.html` عبر http/https.

---

## 3) هيكلة المشروع
```
restructured_step184/
  index.html
  components/        (nav + modals)
  pages/             (واجهات الصفحات كـ Views)
  assets/
    css/             (ستايل موحّد + تحسينات الجداول/الأزرار)
    js/
      bootstrap.js   (تحميل pages/components ثم تحميل السكربتات)
      ui_unified.js  (تحسينات UI موحّدة)
      ui_plus.js     (تحسينات إضافية للفلاتر/الحالة/التبويبات…)
      router.js      (دعم hash اختياري)
      app/
        01_core.js   (التخزين + showView/initAll + helpers)
        03_dashboard.js
        04_properties.js
        05_leases.js
        06_tenants.js
        07_payments.js
        08_cheques.js
        09_expenses.js
        10_salaries.js
        11_receipts.js
        12_reports.js
        13_settings_init_ui.js
        14_police_cases.js
  Attachments/        (مجلد اختياري لحفظ المرفقات فعليًا — يتم إنشاؤه/اختياره من داخل النظام)
```

---

## 4) التخزين والنسخ الاحتياطي (Data Storage)
### أين تُحفظ البيانات؟
- يتم حفظ البيانات داخل المتصفح عبر **localStorage** تحت مفتاح رئيسي:
  - `re_data_v2`

### كيف أسوي نسخة احتياطية؟
- من صفحة **الإعدادات** ستجد:
  - **تصدير JSON**: ينشئ ملف نسخة احتياطية كامل.
  - **استيراد JSON**: يسترجع البيانات من نسخة احتياطية.

### توصية مهمة
- اعمل تصدير نسخة احتياطية بشكل دوري (يومي/أسبوعي) وخزّنها على الـ NAS.
- إذا تم مسح بيانات المتصفح أو تغيير جهاز/متصفح، ستحتاج للاستيراد لاسترجاع البيانات.



### 4.1) المرفقات (File Upload)
> ⚠️ **تشغيل NAS عبر HTTP:** Chrome يمنع الوصول لمجلدات الجهاز وكتابة الملفات على HTTP. لذلك على HTTP سيتم تعطيل (الرفع/الحفظ داخل المسار + استعراض المجلد)، ويعمل فقط **عرض/فتح المسارات المسجلة** (Recorded Mode). لتفعيل الرفع الحقيقي استخدم **HTTPS على الـ NAS** أو شغّل على **localhost (Live Server)**.

- النظام يدعم **رفع وحفظ الملفات فعليًا** (PDF/Images) داخل مجلد تختاره أنت.
- عند أول رفع، سيطلب منك المتصفح **تحديد مجلد المرفقات** ومنح الصلاحية. بعد ذلك يتم الحفظ تلقائيًا.
- يتم إنشاء مسارات منظمة تلقائيًا مثل:
  - `Attachments/tenants/<tenant_key>/id_card/...`
  - `Attachments/tenants/<tenant_key>/trade_license/...`
  - `Attachments/leases/<contract_no>/...`
  - `Attachments/units/<unit_id>/...`
  - `Attachments/cases/<case_id>/...`
  - `Attachments/cheques/<cheque_id>/...`
  - `Attachments/expenses/<expense_id>/...`
  - `Attachments/vouchers/<source_type>/<voucher_id>/...`

> ملاحظة: هذه الخاصية تعتمد على دعم المتصفح لـ **File System Access API** (تعمل أفضل على Chrome/Edge).

---

## 5) شرح الصفحات والخصائص

### 5.1 لوحة المعلومات (Dashboard)
- ملخصات سريعة للحالة العامة (وحدات/عقود/مستحقات/مؤشرات).
- أدوات عرض مختصرة لتسريع القرار.

### 5.2 العقارات والوحدات
- إضافة/تعديل/حذف العقارات.
- إدارة الوحدات التابعة لكل عقار.
- حالات الوحدات (شاغرة/مؤجرة… حسب تعريف النظام لديك).
- ربط الوحدات بالمستأجرين والعقود عند الإنشاء.

#### ✅ مرفقات الوحدة (رفع وحفظ تلقائي)
- داخل مودال الوحدة يوجد زر **رفع** لرفع ملفات تخص الوحدة.
- يتم الحفظ تلقائيًا داخل `Attachments/units/<unit_id>/...` ويتم عرضها كرابط “فتح”.
- يوجد زر **📁 عرض الملفات** داخل مودال الوحدة لعرض كامل مجلد المرفقات.
- وفي جدول الوحدات داخل صفحة العقارات ستجد أيقونة **📁** لعرض مجلد الوحدة بالكامل.

### 5.3 سجل المستأجرين
- عرض قائمة المستأجرين وتجميع بياناتهم من العقود والوحدات.
- بيانات تواصل: هاتف/إيميل… إلخ.
- تمييز شركة/فرد حسب بياناتك.

#### ✅ مرفقات المستأجر (رفع وحفظ تلقائي)
تم إضافة حقول:
- **صورة/ملف الهوية** (idCard)
- **الرخصة التجارية** (tradeLicense)

> عند الضغط على زر **رفع** سيتم اختيار الملف ثم حفظه تلقائيًا داخل مجلد المرفقات (مع إنشاء مسار خاص بكل مستأجر)، ويتم تسجيل **Name + Path** داخل بيانات المستأجر مع زر “فتح”.

---

### 5.4 إدارة العقود (Single & Multi-unit)
- إنشاء عقد **مفرد** أو **متعدد الوحدات**.
- تجديد/إلغاء/إخلاء.
- طباعة وإنشاء بيانات العقد بشكل منظم.

#### ✅ أرشفة العقود (Manual Archive)
- صفحة مستقلة: **أرشيف العقود**
- الأرشفة تتم **يدويًا** فقط عند:
  - التجديد
  - الإلغاء/الإخلاء
- زر **إلغاء الأرشفة** داخل الأرشيف لاستعادة العقد إلى النشط.

#### ✅ مرفق العقد الموثّق في البلدية (رفع وحفظ تلقائي)
- داخل **إضافة عقد** و **تعديل عقد** يوجد زر **رفع** لرفع ملف العقد الموثّق، ويتم حفظه تلقائيًا داخل:
  - `Attachments/leases/<contract_no>/...`
  ويتم تسجيل المسار داخل العقد لسهولة فتحه لاحقًا.
  - زر “اقتراح” لمسار افتراضي سريع
  - رابط “فتح” عند توفر المسار

---

### 5.5 سجل الدفعات المالية (Payments)
- تتبع الدفعات المجدولة للعقود.
- التحويل إلى “مدفوع” عند الدفع الفعلي عبر زر **تسجيل**.
- عند التسجيل يتم إنشاء **سند** (Receipt) للدفع الحقيقي فقط (ليس عند الجدولة).

### 5.6 إدارة الشيكات (Cheques)
- توليد شيكات/جدولة عند إضافة/تجديد العقد.
- حالات الشيك: بانتظار الدفع / بانتظار الصرف / مصروف … (حسب النظام).
- تحسينات UI:
  - جدول منسق ومحاذاة محسّنة
  - زر تعديل أيقونة فقط
  - قائمة الحالة ثابتة الحجم ولا تتمدد

### 5.7 السندات (Receipt + Receipts History)
- إصدار سند قبض/صرف عند السداد الفعلي.
- سجل شامل للسندات مع بحث وفرز.

### 5.8 المصروفات
- تسجيل مصروفات العقار/التشغيل.
- تتبع المبالغ والتواريخ وملاحظات.

### 5.9 رواتب الحارس والعمال
- تسجيل الرواتب والمستحقات المرتبطة.

### 5.10 التقارير الشاملة
- صفحات تقارير لتجميع البيانات وإظهار ملخصات وسجلات.
- أدوات بحث وفرز وفلاتر حسب المحتوى.

### 5.11 الإشعارات والإنذارات
- إنشاء/عرض إنذارات وبلاغات مرتبطة بالمتأخرات والالتزامات.
- طباعة بصيغة A4 مع تحسين الهوامش والمحاذاة (جاهز للطباعة).

---

## 6) البلاغات والدعاوى القضائية (Police & Court Cases) ✅
صفحة جديدة باسم: **بلاغات الشرطة والدعاوى القضائية**

### 6.1 بيانات القضية/البلاغ
- رقم البلاغ/القضية
- الجهة/المحكمة
- التاريخ
- الحالة (مفتوحة/قيد الإجراء/مغلقة)
- ربط اختياري بـ:
  - عقار / وحدة / مستأجر / عقد

### 6.2 تطورات القضية (Timeline / Updates)
- إضافة تطور من الأنواع:
  - مذكرة/محضر
  - رسوم وغرامات (مع مبلغ)
  - حكم/قرار
  - مستند

### 6.3 المواعيد والتنبيهات
- إضافة موعد “مراجعة شرطة” أو “جلسة محكمة”
- تحديد:
  - تاريخ + (وقت اختياري)
  - التنبيه قبل الموعد (مثلاً 7/3/1 يوم)
  - “تم” لإغلاق الموعد

> التنبيه يظهر داخل النظام طالما الصفحة/النظام مفتوح، ويتم منع التكرار لنفس اليوم لكل (موعد + عدد أيام).

### 6.4 مرفقات القضايا (رفع + عرض مجلد)
- يمكنك رفع مرفقات للقضية نفسها + مرفقات لكل تطور Timeline.
- يتم الحفظ تلقائيًا داخل `Attachments/cases/<case_id>/...`.
- في جدول القضايا ستجد أيقونة **📁** لعرض مجلد القضية بالكامل.
- داخل لوحة تفاصيل القضية يوجد زر **📁 الملفات** لعرض نفس المجلد.


## 7) نظام المرفقات (File Upload + Folder Viewer) — مهم للـ NAS
- النظام يدعم **رفع الملفات من داخل الصفحات** ثم **حفظها فعليًا داخل مجلد تختاره** (Attachments Root) باستخدام **File System Access API** (أفضل دعم: Chrome/Edge).
- في المتصفحات التي لا تدعم الكتابة على مجلدات الجهاز، يتم حفظ الملفات داخل **OPFS** (تخزين داخلي للمتصفح) كبديل.
- ميزة **📁 عرض الملفات** تعرض محتويات مجلد الكيان بالكامل (حتى الملفات التي لم تُسجل داخل JSON).

### 7.1 الهيكلة التلقائية للمجلدات
```
Attachments/
  tenants/<tenant_key>/
    id_card/...
    trade_license/...
  leases/<contract_no>/
    municipality/...
  units/<unit_id>/...
  cases/<case_id>/...
```

### 7.2 النسخ الاحتياطي
- **تصدير/استيراد JSON** يحفظ بيانات النظام + مسارات المرفقات.
- **الملفات نفسها** لا تدخل داخل JSON؛ لذلك احتفِظ أيضًا بنسخة من مجلد `Attachments/` (أو من بيانات OPFS إذا كنت تستخدم متصفح لا يدعم FS Access).

### 7.3 توصية للـ NAS
- ضع المشروع + مجلد `Attachments/` في نفس المسار على الـ NAS (أو اختر مجلد Attachments على مشاركة الشبكة).
- استخدم مسارات نسبية قدر الإمكان لضمان عمل الروابط بعد نقل المشروع.



## 8) الفلاتر المتقدمة والبحث (Unified UI)
- زر موحّد: **الفلاتر المتقدمة**
- الفلاتر مخفية افتراضيًا وتظهر عند الضغط.
- زر **إعادة ضبط** داخل لوحة الفلاتر فقط.
- تم ضبط سلوك الزر بحيث لا يؤثر على صفحات لا تحتوي لوحة فلاتر.

---

## 9) استكشاف الأخطاء (Troubleshooting)
- **صفحة بيضاء / لم تُحمّل المكونات:** تأكد أنك على http/https وليس file://  
- **فقدان بيانات:** البيانات في localStorage — صدّر نسخة احتياطية من صفحة الإعدادات بشكل دوري.
- **Tailwind CDN Warning:** التحذير طبيعي للتطوير المحلي، ويمكن لاحقًا تحويله لنسخة production عبر Tailwind CLI إذا رغبت.

---

## 10) سجل التغييرات (Changelog)
### UIV32.10
- إضافة صفحة البلاغات والدعاوى القضائية + ربط Router/Sidebar + مودالات أساسية.

### UIV32.11
- دعم المرفقات كـ “مسارات” للقضايا + تنظيم مجلد Attachments.

### UIV32.12
- إضافة مرفقات للمستأجرين (هوية/رخصة) + مرفق العقد الموثق في البلدية.
- تحسينات تكامل وحفظ/استيراد المفاتيح الجديدة.

### UIV32.13 (Final)
- تدقيق شامل للمشروع:
  - فحص Syntax لكل ملفات JS.
  - التحقق من توفر جميع السكربتات وتحميلها عبر bootstrap.
  - إصلاح ربط الدالة `onCasesAdvancedChangedDebounced()` لتكون متاحة للـ inline handlers.
- تحديث README بشكل مفصل.

---

### © ملاحظة
هذا المشروع يعمل محليًا، ولا يرسل بيانات لأي جهة خارجية.  
استخدامك له داخل جهازك/شبكتك مسؤوليتك، وينصح بعمل نسخ احتياطية منتظمة.


---

## 9) إدارة المرفقات والملفات (رفع وحفظ داخل مجلدات)

### 9.1 الفكرة
المشروع يعمل بدون Back-End، لذلك **المتصفح لا يسمح** بالحفظ إلى مجلدات الجهاز تلقائيًا إلا عبر صلاحية يختارها المستخدم.
تمت إضافة آلية جديدة تُمكّنك من:

- تحديد **مجلد المرفقات** مرة واحدة من داخل النظام (زر: **تحديد مجلد المرفقات**).
- رفع الملفات مباشرة من الصفحات/المودالات وحفظها داخل المجلد المختار.
- إنشاء مسار/مجلد خاص لكل:
  - **مستأجر**: `Attachments/tenants/<tenantKey>/...`
  - **وحدة**: `Attachments/units/<unitId>/...`
  - **قضية/بلاغ**: `Attachments/cases/<caseId>/...`
  - **عقد** (ملف العقد الموثق في البلدية): `Attachments/leases/<contractNo>/...`
  - **شيك**: `Attachments/cheques/<chequeId>/...`
  - **مصروف**: `Attachments/expenses/<expenseId>/...`
  - **سند/إيصال**: `Attachments/vouchers/<sourceType>/<voucherId>/...`

> ✅ الأفضل أن تختار مجلد المشروع نفسه:  
> `.../restructured_step184/Attachments`  
> حتى تكون المسارات داخل المشروع واضحة ومتسقة.

### 9.2 المتصفحات المدعومة
- **Chrome / Edge (Windows/Mac)**: يدعم File System Access API ⇒ حفظ الملفات فعليًا داخل المجلد الذي تختاره.
- **Firefox / Safari**: غالبًا لا يدعم الكتابة لمجلدات الجهاز؛ سيستخدم النظام تخزين داخلي (OPFS) — الملفات تُحفظ ولكن **لن تراها كمجلدات على النظام**.

### 9.3 ملاحظات مهمة للنسخ الاحتياطي
- تصدير/استيراد JSON يحفظ **البيانات + مسارات الملفات** فقط.
- **الملفات نفسها** تحتاج نسخها يدويًا عن طريق نسخ مجلد `Attachments` (إن كنت تستخدم Chrome/Edge وتحفظ داخل مجلد ظاهر).
- إذا كنت تستخدم OPFS (Safari/Firefox)، فالملفات داخل تخزين المتصفح ولن تُنسخ ضمن ملفات المشروع.

### 9.4 كيف أستخدم الرفع؟
- داخل **سجل المستأجرين**: سترى حقول وزر لرفع
  - صورة الهوية
  - الرخصة التجارية
- داخل **العقود**: زر رفع ملف العقد الموثق في البلدية.
- داخل **بلاغات الشرطة والدعاوى**:
  - زر رفع مرفقات للقضية
  - زر رفع مرفقات للتطورات (Updates)
- داخل **الوحدات**:
  - قسم “مرفقات الوحدة” مع رفع متعدد وإدارة قائمة المرفقات.



### 9.5 عرض المرفقات (Viewer)
تمت إضافة مودال عام لعرض **كل الملفات داخل مجلد المرفقات** لأي كيان، مع معاينة سريعة (صور/PDF) وزر فتح ونسخ المسار.

- داخل **سجل المستأجرين**: ستجد أيقونة **📁** في عمود المرفقات + زر **ملفات**.
- داخل **صفحة الشيكات**: أيقونة **📁** لعرض مجلد مرفقات الشيك + زر **⬆️** للرفع.
- داخل **صفحة المصروفات**: أيقونة **📁**/⬆️ لكل مصروف + أزرار رفع/عرض داخل نموذج إضافة المصروف.
- داخل **سجل السندات**: أيقونة **📁**/⬆️ لكل سند + أزرار مرفقات داخل صفحة عرض السند.
- داخل **الشيكات**: تمت إضافة أزرار **📁 عرض** و **⬆️ رفع** لكل شيك.
- داخل **المصروفات**: تمت إضافة أزرار **📁 عرض** و **⬆️ رفع** لكل مصروف + داخل نموذج إضافة مصروف.
- داخل **سجل السندات (القبض/الصرف)**: تمت إضافة أزرار **📁 عرض** و **⬆️ رفع** لكل سند، وكذلك داخل صفحة معاينة السند.
- داخل **العقود**: ستجد أيقونة **📁** بجانب رقم العقد لعرض مجلد العقد بالكامل.
- داخل **الوحدات**: ستجد أيقونة **📁** في جدول الوحدات لعرض مجلد الوحدة بالكامل.
- داخل **القضايا**: ستجد أيقونة **📁** في جدول القضايا وزر **📁 الملفات** داخل التفاصيل.
- داخل المودالات (المستأجر/العقد): يوجد رابط **عرض كل الملفات**.

داخل شاشة العرض:
- زر **تحديد مجلد المرفقات** (عند أول مرة أو عند تغيير المجلد)
- زر **تحديث** لإعادة قراءة المجلد
- اضغط على اسم الملف لعرض معاينة، أو اضغط **فتح** لفتحه في تبويب جديد.


---



### 5.13 بلاغات الشرطة والدعاوى القضائية
- تسجيل البلاغات والقضايا وربطها (عقار/وحدة/مستأجر/عقد) عند الحاجة.
- إدارة تطورات القضية (Timeline) + رسوم/غرامات + أحكام.
- مواعيد وجلسات مع تنبيهات قبل الموعد.

#### ✅ مرفقات القضايا (رفع وحفظ تلقائي)
- داخل مودال القضية ومودال التطورات يوجد زر **رفع** لحفظ المستندات تلقائيًا داخل `Attachments/cases/<case_id>/...`.


## ملاحظة مهمة (Offline)
- تمت إضافة ملف `assets/css/tw_fallback.css` كـ **شبكة أمان** في حال عدم تحميل Tailwind من CDN.
- هذا يمنع ظهور كل الصفحات فوق بعضها (ويحل مشاكل الـ scroll/التجميد) لأن النظام يعتمد على `.hidden` لإخفاء الـ views والمودالات.
- يظل الأفضل لاحقاً استبدال Tailwind CDN بنسخة محلية كاملة عند تجهيز بيئة NAS بدون إنترنت.
